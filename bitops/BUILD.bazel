load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

# ── C implementation ─────────────────────────────────────────

cc_library(
    name = "gen_bitops",
    srcs = ["src/bitmap.c"],
    hdrs = ["include/gen_bitops.h"],
    includes = ["include"],
    copts = [
        "-ffreestanding",
        "-nostdlib",
        "-std=c17",
        "-Wall",
        "-Wextra",
        "-Werror",
        "-Wpedantic",
    ],
    deps = ["//genesis-abi:genesis_abi"],
)

cc_test(
    name = "bitops_test",
    srcs = ["tests/bitops_test.c"],
    deps = [":gen_bitops"],
    copts = [
        "-std=c17",
        "-Wall",
        "-Wextra",
        "-Werror",
    ],
)

# ── Rust implementation ──────────────────────────────────────

rust_library(
    name = "kernel_bitops",
    srcs = ["src/lib.rs"],
    crate_name = "kernel_bitops",
    edition = "2021",
    deps = ["//genesis-abi:genesis_abi_rs"],
)

rust_test(
    name = "kernel_bitops_test",
    crate = ":kernel_bitops",
)

exports_files(["Cargo.toml"])

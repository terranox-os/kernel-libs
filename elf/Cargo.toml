[package]
name = "kernel-elf"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Freestanding ELF32/ELF64 parser: headers, sections, symbols, relocations"

[dependencies]
genesis-abi = { path = "../genesis-abi" }

[package]
name = "kernel-alloc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Freestanding bump allocator for kernel early-init and scratch allocation"

[dependencies]
genesis-abi = { path = "../genesis-abi" }
